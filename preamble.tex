%% =============================================================
%%  Shared preamble for main.tex and supplementary.tex.
%%  main.tex uses the OUP class and supplementary.tex uses `article`,
%%  so ONLY class-agnostic packages/macros belong here; class-specific
%%  packages stay in each document. Add shared \usepackage or
%%  \newcommand definitions here so the two documents cannot drift.
%% =============================================================

\usepackage{microtype}
\graphicspath{{figures/}}
