% RIsearch3 system workflow — TikZ source
% Included via \input{figures/workflow} inside a figure* environment in body.tex
\usetikzlibrary{arrows.meta,positioning,shapes.multipart,backgrounds,fit}

\definecolor{RustRed}{RGB}{206,66,43}
\definecolor{PythonBlue}{RGB}{55,118,171}
\definecolor{PipeTeal}{RGB}{25,138,128}
\definecolor{IODark}{RGB}{85,85,85}

\begin{tikzpicture}[
  >=Stealth,
  font=\sffamily\scriptsize,
  node distance=0.5cm,
  every node/.style={align=center},
  %% shared box skeleton
  basebox/.style={
    rectangle split, rectangle split parts=2,
    rounded corners=5pt,
    inner sep=4pt,
  },
  %% per-role styles
  iobox/.style={basebox,
    draw=IODark,
    rectangle split part fill={IODark!75, IODark!7},
    minimum width=2.2cm, text width=2.0cm,
  },
  rustbox/.style={basebox,
    draw=RustRed,
    rectangle split part fill={RustRed!82, RustRed!7},
    minimum width=3.1cm, text width=2.9cm,
  },
  pybox/.style={basebox,
    draw=PythonBlue,
    rectangle split part fill={PythonBlue!82, PythonBlue!7},
    minimum width=3.1cm, text width=2.9cm,
  },
  pipebox/.style={basebox,
    draw=PipeTeal,
    rectangle split part fill={PipeTeal!82, PipeTeal!7},
    minimum width=4.6cm, text width=4.4cm,
  },
  %% arrows
  marrow/.style={->, thick, draw=black!50},
  annot/.style={
    midway, above=3pt,
    font=\sffamily\tiny\itshape,
    fill=white, inner xsep=2pt, inner ysep=1pt,
    text width=1.9cm,
  },
]

%% ── NODES ──────────────────────────────────────────────────────────────────

\node[iobox] (input) {
  \textcolor{white}{\bfseries\small Input}
  \nodepart{two}
  Query siRNAs\\(FASTA)\\[4pt]
  Target\\transcriptome\\(FASTA)\\[4pt]
  GTF\,/\,BED\\annotation
};

\node[rustbox, right=0.55cm of input] (core) {
  \textcolor{white}{\bfseries\small RIsearch3 Core}\\[1pt]
  \textcolor{white}{\tiny Rust}
  \nodepart{two}
  \ttfamily risearch~index\\[2pt]
  risearch~search\\[5pt]
  \sffamily
  Suffix-array index\\
  t04\,/\,t99 energy params\\
  X-drop DP heuristic\\[3pt]
  2--3$\times$ vs.\ RIsearch2
};

\node[pybox, right=0.55cm of core] (py) {
  \textcolor{white}{\bfseries\small Python Bindings}\\[1pt]
  \textcolor{white}{\tiny PyO3\,$\cdot$\,Maturin}
  \nodepart{two}
  \ttfamily risearch.index()\\[2pt]
  risearch.search()\\[5pt]
  \sffamily
  $\Rightarrow$\,Polars DataFrame\\
  Arrow\,C\,Stream\\
  (zero-copy)\\[3pt]
  \ttfamily pip install risearch3
};

\node[pipebox, right=0.55cm of py] (pipe) {
  \textcolor{white}{\bfseries\small siRNA Off-Target Pipeline}\\[1pt]
  \textcolor{white}{\tiny Python\,$\cdot$\,Polars\,$\cdot$\,YAML}
  \nodepart{two}
  \sffamily
  \textbf{1.}\ Search via bindings\\[2pt]
  \textbf{2.}\ Intersection (NCLS)\\[2pt]
  \textbf{3.}\ Accessibility\\
  \hspace*{10pt}(ViennaRNA API)\\[2pt]
  \textbf{4.}\ Partition function $Z$\\[4pt]
  \ttfamily off-targets\quad accessibility
};

\node[iobox, right=0.55cm of pipe] (output) {
  \textcolor{white}{\bfseries\small Output}
  \nodepart{two}
  $p_{\mathrm{off}}$ per transcript\\[4pt]
  $P_{\mathrm{OFF}}$ aggregate\\[4pt]
  TSV\,$\cdot$\,CSV\,$\cdot$\,Parquet
};

%% ── ARROWS ──────────────────────────────────────────────────────────────────

\draw[marrow] (input.east)   -- (core.west);
\draw[marrow] (core.east)    -- (py.west);
\draw[marrow] (py.east)      --
  node[annot]{Polars DataFrame\\[-1pt](in-process)}
  (pipe.west);
\draw[marrow] (pipe.east)    -- (output.west);

\end{tikzpicture}
